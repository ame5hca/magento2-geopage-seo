<?php
$viewModel = $block->getParentBlock()->getViewModel();
if($viewModel->showNextDayDeliveryInfo()):
    $content = $viewModel->getNextDayDeliveryContent();
    if ($content):
?>
        <div class="mainContainer block-content next-day-delivery-info width-80">
            <div class="block-title">
                <h2><?= $viewModel->getNextDayDeliveryBlockTitle() ?></h2>
                <div class="heading-divider heading-divider--short"></div>
            </div>
            <div class="content">
                <p><?= $content ?></p>
            </div>
        </div>
    <?php endif; ?>
<?php endif; ?>